export const FullScreen = (data, type) => {
  return {
    type: type,
    payload: data,
  };
}

export const FullScreenVideo = (data, type) => {
  return {
    type: type,
    payload: data,
  };
}
